{
  "name": "hoseo-lms-plus",
  "private": true,
  "version": "1.2.2",
  "scripts": {
    "lint": "eslint . --ext .js",
    "test": "node --test",
    "build": "rm -rf dist && mkdir -p dist && cp -r manifest.json content.js styles.css lib/ icon*.png dist/ && cd dist && zip -r hoseo-lms-plus-v$(node -p \"require('../package.json').version\").zip manifest.json content.js styles.css lib/ icon*.png"
  },
  "devDependencies": {
    "eslint": "^9.23.0",
    "globals": "^15.15.0",
    "jsdom": "^26.0.0"
  }
}
