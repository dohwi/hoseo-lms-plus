{
  "name": "hoseo-lms-plus",
  "private": true,
  "version": "1.4.2",
  "scripts": {
    "lint": "eslint .",
    "lint:firefox": "npm run build:firefox && web-ext lint -s dist/firefox",
    "test": "node --test",
    "build": "node scripts/build.js chrome",
    "build:chrome": "node scripts/build.js chrome",
    "build:firefox": "node scripts/build.js firefox",
    "build:all": "node scripts/build.js all",
    "verify:zip": "node scripts/zip-policy.js"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "jsdom": "^26.0.0",
    "web-ext": "^10.6.0"
  }
}
