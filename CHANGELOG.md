# 변경 이력

## [Unreleased]

---

## [1.4.2] - 2026-09-15

### 추가
- 강좌별 `/mod/ubboard/view.php` 공지 게시판에서 개별 공지글을 자동 수집
- 공지 작성일을 강좌 주차 기간에 매칭하고, 매칭되지 않은 공지는 `기타`로 분류
- 공지사항을 전체 학습 자료 표에 확성기 아이콘과 중립 항목으로 표시
- 주차별 `전체 자료`, `완료`, `미완료`, `공지` 요약 영역 추가
- 마감 7일 이내 미완료 항목에 `마감 임박` 배지와 테두리 강조 추가
- 개인정보처리방침 및 대시보드 하단 이용 안내 추가
- 캐시 스키마·크기 검증, 응답 크기 제한, 로그인 폼 기반 세션 만료 감지 추가
- ZIP allowlist·압축 해제·CRC·재현성 검증 및 런타임 의존성 보안 감사 CI 추가

### UI/접근성
- 공지사항 행에 실제 마감 임박 행과 같은 구조의 굵은 노란 테두리와 옅은 배경 적용
- 표기 기준 안내를 중앙 모달로 개편하고 실제 행 디자인과 유사한 상태 미리보기 제공
- 표기 기준 모달에 Escape 닫기, 배경 클릭 닫기, Tab 포커스 트랩 및 포커스 복귀 적용
- 모달이 열리면 뒤쪽 대시보드에 `inert`와 `aria-hidden`을 적용하고 종료 시 복원
- `rowspan`으로 강좌명이 생략된 후속 행에 스크린 리더용 강좌명 문맥 추가
- `호서 LMS+` 제목과 버전 배지의 수평 정렬 및 모바일 헤더 배치 개선
- 요약 영역의 강한 상단 컬러 라인을 제거하고 각진 저자극 디자인으로 변경
- `[시작 전]` 배지는 제거하고 시작 전 상태는 행 색상으로만 구분
- 경고 목록을 접고 펼칠 수 있는 `<details>` 구조로 변경
- 빈 주차와 빈 학습 자료 상태의 안내 표시 개선

### 보안
- LMS 링크와 네트워크 요청을 `https://learn.hoseo.ac.kr` 동일 출처로 제한
- `javascript:`, `data:`, 외부 링크 및 외부 이미지 리소스를 정제 단계에서 차단
- LMS 응답 HTML을 허용 태그·속성 화이트리스트로 정제한 뒤 렌더링
- 사용자 식별 실패 시 캐시를 사용하지 않아 공용 환경에서의 캐시 충돌 방지
- 캐시 페이로드를 4 MiB로 제한하고 손상되거나 오래된 캐시를 자동 제거
- LMS 응답을 2 MiB로 제한해 비정상적으로 큰 페이지의 파싱·메모리 사용 방지
- GitHub Actions를 공식 저장소의 검증된 커밋 SHA로 고정
- 배포 ZIP에서 개발 파일, 민감 파일, 경로 순회 및 중복 엔트리를 자동 차단

### 성능/신뢰성
- 모든 LMS 요청에 15초 타임아웃과 페이지 이탈 시 일괄 취소 적용
- 요청 큐의 최대 동시 실행 수를 6개로 제한해 순간적인 서버 부하 방지
- 한 번의 대시보드 로딩에서 동일 URL의 진행 중·완료 요청 결과 재사용
- 하드 요청 개수 제한 없이 모든 미완료 과제·퀴즈의 상세 상태 확인
- 공지 파싱 후보 수와 제목·날짜 입력 길이를 제한해 비정상 게시판 처리 비용 제어
- 주차 이동 시 기존 주차별 활동 Map을 재사용해 반복 그룹화 제거
- 확장 저장소 용량 초과 시 오래된 캐시를 정리하고 안전하게 재시도
- 로그인 URL뿐 아니라 로그인 폼 HTML도 감지해 세션 만료 처리 강화
- 일부 강좌 요청 실패와 요청 취소를 구분해 불필요한 경고 방지

### 빌드/배포
- Chrome 및 Firefox 배포본을 Node 내장 `zlib` 기반 결정적 ZIP으로 생성
- 파일 순서와 타임스탬프를 고정해 동일 소스의 SHA-256 체크섬 재현
- ZIP 생성 직후 allowlist, Local Header, 압축 데이터, 크기 및 CRC-32 검증
- Chrome/Firefox manifest 생성 시 package·lockfile·manifest 버전 일치 검증
- PR 및 main push에서 린트, 테스트, 보안 감사, 빌드, ZIP 검증, Firefox 사전 검사 수행
- GitHub Release는 명시적인 수동 실행에서만 생성하도록 변경
- 동일 버전 릴리스가 이미 존재하면 자산을 덮어쓰지 않도록 보호

### 수정
- 연말연시를 넘는 주차 기간의 연도 계산 오류 수정
- 공지 링크 파라미터와 날짜 형식 지원을 확대해 누락 가능성 감소
- 정규 주차에 매칭되지 않은 공지가 임의의 현재 주차에 표시되던 동작 제거
- 과제·퀴즈·강좌 자료의 위험하거나 외부인 URL을 요청하지 않도록 수정
- 렌더링과 페이지 전환 과정의 키보드 이벤트 및 모달 정리 누수 방지
- 일반 `userid` 폼을 로그인 폼으로 오인하던 세션 만료 판정 가능성 제거
- UTF-8 크기 계산에서 이모지 등 surrogate pair를 정확히 처리
- 활동 정렬을 강좌·주차·유형 기준으로 안정화해 `rowspan` 구조 유지

### 의존성
- ESLint, globals, web-ext 개발 의존성 업데이트
- actions/checkout, actions/setup-node, softprops/action-gh-release 업데이트

---

## [1.4.1] - 2026-06-11

### 변경
- CI에 main push 시 자동 태그 생성 및 GitHub Release 배포 추가
- Chrome/Firefox 빌드 아티팩트 업로드

---

## [1.4.0] - 2026-06-11

### 리팩터링
- `lib/ui.js`를 5개 모듈로 분할 (elements, dates, tooltip, render, index)
- `manifest.json`의 content_scripts 로딩 순서 반영
- 테스트에서 서브 모듈을 명시적 로드 후 집계 모듈 사용

---

## [1.3.0] - 2026-06-11

### 변경
- `core.buildActivityKey`가 빈 문자열 대신 `null` 반환 — 매칭 로직 타입 안전성 강화
- `core.dedupActivities`가 null 키 항목을 필터링하도록 개선
- `core.SELECTORS`에 파서용 셀렉터 fallback 체인 추가 (attendanceTable, courseSection, activityTable, quizTable)
- `core.queryWithFallback()` 유틸 추가 — 배열 셀렉터 순회하며 첫 매치 사용
- `IRREGULAR_COURSE_TYPES`를 `core.SELECTORS.irregularCourseTypes`로 이전
- `lib/parsers.js`의 모든 셀렉터를 `queryWithFallback`으로 교체
- `lib/data-service.js`의 `findMatchedByKey`에 null 키 가드 추가

---

## [1.2.9] - 2026-06-11

### 추가
- `docs/architecture.md` 신규 (모듈 의존성, 캐시 스키마, 매칭 정책, 셀렉터 가이드)
- `lib/types.js`의 `@typedef`에 의도 및 fallback 설명 보강
- `lib/core.js` 핵심 함수에 의도 주석 추가
- `lib/data-service.js` 상단에 매칭 정책 2단계 명세

---

## [1.2.8] - 2026-06-11

### 리팩터링
- 활동/과제/출석 항목 매칭 키를 `core.buildActivityKey`로 통합
- `data-service`의 `isSameActivity` 제거, 매칭 키 계산 로직 중복 해소

---

## [1.2.7] - 2026-06-11

### 수정
- SPA 네비게이션 시 대시보드/사이드바가 잔존하던 문제 수정
- `popstate` 및 `<title>` 변경 감지로 LMS 내부 화면 전환 대응
- 대시보드 정리 시 기존 LMS 메인 컨텐츠를 복원하도록 변경

---

## [1.2.6] - 2026-06-11

### 변경
- 대시보드 버전 표기를 manifest.json과 자동 연동하도록 변경
- lib/ui.js의 하드코딩 버전 문자열(`v1.2.5`) 제거

---

## [1.2.2] - 2026-03-27

### 추가
- Firefox 배포용 패키지 빌드 및 사전 검사 스크립트 추가
- 상태 표기 기준 안내 툴팁과 하단 안내 문구 추가

### 변경
- Chrome/Firefox 공통 확장 API 사용 방식으로 정리
- 퀴즈 연속 행 파싱과 주차 매칭 정확도 개선
- 미완료 항목 강조 기준을 7일 이내로 조정하고 시작 전 항목은 노란색으로 표시
- 설치 안내를 Chrome 웹스토어 설치와 수동 설치로 구분

---

## [1.2.1] - 2026-03-24

### 추가
- 헤더에 버전 뱃지 (v1.2.1) 추가
- 마감 5일 이하 항목 자동 감지 및 강조 표시 기능
  - 붉은 테두리로 시각적 강조
  - 마감일 텍스트 빨간색 표시
  - 미수강 섹션: 해당 주차 ~ 이수여부까지 붉은 테두리
  - 통합본: 유형 ~ 이수여부까지 붉은 테두리

### 변경
- 대시보드 헤더를 3단 레이아웃으로 개선 (타이틀 - 주차 선택기 - 액션 버튼)
- 주차 선택기를 헤더 중앙으로 이동
- 주차 기간 정보를 "전체 학습 자료" 섹션 위로 이동
- 새로고침 버튼에 🔄 이모지 추가
- 미수강 섹션 스타일을 전체 학습 자료 섹션과 통일

### 제거
- "LMS 홈으로 복귀" 버튼 제거
- 기존 독립 주차 카드 섹션 제거
- 미수강 섹션의 불필요한 흰 배경 제거

### 최적화
- CSS 스타일 정리 및 중복 제거
- 헤더 레이아웃 최적화

---

## [1.2.0] - 이전 버전
(이전 버전 내역은 Git 히스토리 참조)
